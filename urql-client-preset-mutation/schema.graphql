type Query {
    user(id: ID!): User!
}

type Mutation {
    updateUser(user: UserInput!): User!
}

input UserInput {
    id: ID!
    username: String
    email: String
}

type User {
    id: ID!
    username: String
    email: String!
}


